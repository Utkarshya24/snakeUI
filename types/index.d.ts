// Re-exporting all component prop types for external TypeScript users.
// This allows users to import types directly from 'snakeui/types' for better DX.
export type {
    SnakeTableProps,
    SnakeHeadProps,
    SnakeBodyProps,
    SnakeRowProps,
    SnakeCellProps,
} from './src/components/table/types';
  